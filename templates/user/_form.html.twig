{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="container-btn-maj-cancel">
    <button class="btn ml-2">{{ button_label|default('Sauvegarder') }}</button>
    <a href="{{ path('user_profile_show') }}">Annuler</a>
</div>

{{ form_end(form) }}
